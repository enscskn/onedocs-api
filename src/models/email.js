// Email status enum
const EmailStatus = {
  PENDING: 'pending',
  IN_PROGRESS: 'in_progress',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
};

// Email model örneği (Supabase tablosu ile eşleşmeli)
// Bu dosya, validasyon ve örnek veri için kullanılabilir

module.exports = { EmailStatus }; 