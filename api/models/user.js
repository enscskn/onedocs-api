// User role enum
const UserRole = {
  ADMIN: 'admin',
  USER: 'user',
};

// User model örneği (Supabase tablosu ile eşleşmeli)
// Bu dosya, validasyon ve örnek veri için kullanılabilir

module.exports = { UserRole }; 